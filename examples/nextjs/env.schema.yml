NEXT_PUBLIC_API_URL:
  type: url
  required: true
  public: true
  description: Public API endpoint used by browser code.

DATABASE_URL:
  type: url
  required: true
  description: Server-side database connection URL.

AUTH_SECRET:
  type: string
  required: true
  secret: true
  description: Server-side authentication signing secret.

